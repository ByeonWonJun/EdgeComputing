1. makestatic 파일 -> use **.o file.. (여러가지 object file을 모아서 spnfsd 파일을 생성)
  (**.o파일을 컴파일하여 spnfsd 파일을 생성)
-> 1. 그렇다면 object file을 생성하는 곳은 어디일까?
-> 2. object file은 혹시 gcc에 `mysql_config --libs`를 안붙이고 생성할수 있나? -> 확인결과 안됨

2. object file을 생성하는 위치
- 일단 처음으로 /utils/spnfsd/Makefile.am 파일을 확인해보았음
//-------------------------------------------------------------------
## Process this file with automake to produce Makefile.in
.............................
spnfsd_SOURCES = \		-> 해당 부분에서 c파일을 o파일로 변환하는것이 아닐까 추측중
	atomicio.c \
	spnfsd_ops.c \
	cfg.c \
	spnfsd.c \	-> 이 부분
	strlcat.c \
	strlcpy.c \
	\	-> 헤더 파일의 용도는 잘 모르겠음
	cfg.h \
	spnfsd.h \
	spnfsd_queue.h \
	queue.h

spnfsd_LDADD = -levent ../../support/nfs/libnfs.a	-> 그러면 해당부분에 파일을 추가하면 될까나?? 될지 안될지 모르겠음
.....................						(조금 더 makefile.am과 make에 추가하는 법을 알아본다음에 진행
//-----------------------------------------------------------------
**********************************************************************************************
autoconf
- configure 스크립트를 생성하기위한 명령어
- configure.ac 파일에 있는 설정을 참고하여 configure 파일을 생성
**********************************************************************************************
automake
- Makefile을 만들기위해 필요한 Makefile.in 파일을 생성
**********************************************************************************************
makefile.am
- bin_PROGRAMS=spnfsd
	- 결과적으로 만들어지는 코드
	- 위와 같은 경우, 결과적으로 spnfsd라는 이름의 프로그램을 만드는 코드를 가짐
**********************************************************************************************

3. `mysql_config --libs`등의 문구를 gcc 뒤에 붙이지 않고 사용할수 있는 방법은 없을까?
	(gcc 옵션? gcc 매개변수?)

